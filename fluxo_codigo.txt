Fluxo do state de fonte:
    1 => Os dados são criados em App.jsx. (const [font, setFont] = useState("Arial"))
    2 => Esses dados são passados ao componente "Header" em sua chamada. (<Header font={font} setFont={seFont} />. O que está entre chaves são os valores vindo do App.jsx, o que está antes do sinal "=" se refere aos props que serão passados ao "Header")
    3 => O "Header" por sua vez passa esses props quando chama o componente "ChooseFont". Da mesma forma, o que está dentro das chaves se refere as props de "Cabecalho" e o que está antes de "=" se refere as props de "ChooseFont"
    4 => Em "ChooseFont", as props "font" e "setFont" que foram recebidas por "Header" serão usadas para "x" coisas. 

Organização dos elementos da página:
   1=> Body:
        2=> div "app/arial" está centralizada, com width 900px(min) e 1200px(max). Está dentro de body e guardará todos os outros componentes
            3=> header, 900px(max)
                - img => Logo 
                - Select => Menu que seleciona as fontes
                - Input(checkbox) => Botão pra trocar o tema da página
            4=> SearchBar tem layout grid para controlar o ícone da lupa
                - Input(text) => Campo de pesquisa
                - label => Label da pesquisa é um img(lupa)
            5=> Section tem layout flex para controlar de forma vertical os containers de dentro
                - div(id=word-header) é um container grid
                    - h1 => Guarda a palavra pesquisada
                    - p => Guarda a transcrição fonética da palavra
                    - img => Imagem do play. Quando clicada, toca a pronúncia da palavra
                - div(id=part-of-speech-container) é um container grid que guarda 2 elementos horizontalmente alinhados
                    - p => Guarda a classe gramatical da palavra 
                    - div(id=line) => Somente uma linha horizontal ao lado de p
   